#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;

int main() {
    int a = -1;

    if (a >= 0) {
        if (a > 0)
            cout << "positive";
    } else {
        cout << "negative";
    }
    // negative

    return 0;
}